{layout 'layout.latte'}
{block content}
	To: {$command->getTo()}<br/>
	Fleet: {$command->getNonZeroFleet()|implodeAssoc:',',':'}<br/>
	Mission: {$command->getMission()}<br/>
	Wait for resources: {$command->waitForResources() ? 'yes' : 'no'}
{/block}

{block edit}
	<a class="btn btn-info" href="{plink ManageCommands:sendFleet, uuid => $command->getUuid()->toString()}">Edit</a>
{/block}